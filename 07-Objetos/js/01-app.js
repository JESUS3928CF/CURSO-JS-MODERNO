const nombre = "Monitor de 20 pulgadas";
const precio = 300;
const disponible = true;

// Un objeto agrupa todo en un solo lugar en este caso en una sola variable


// Creando un Object literal

const producto = {
    nombre: "Monitor de 20 pulgadas",
    precio: precio,
    disponible: disponible,
}

console.log(producto);