/// Retorna un NodeList (Array) con todos los elementos encontrados
const card = document.querySelectorAll('.card');
console.log(card);

/// Si no existe Retorne un NodeList vacío (Array)
const noExiste = document.querySelectorAll('.no-existe');
console.log(noExiste);